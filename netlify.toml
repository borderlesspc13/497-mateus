# https://docs.netlify.com/build/configure-builds/file-based-configuration/
# Next.js 16: o adapter OpenNext é aplicado automaticamente (sem plugin pinado).
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
